{
  "name": "singlestore",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "start:examples:estore": "turbo start --filter='./examples/estore'",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/prettier-config": "*",
    "prettier": "^3.2.5",
    "turbo": "^2.0.11",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.2.3",
  "workspaces": [
    "apps/*",
    "examples/*",
    "packages/*"
  ]
}
